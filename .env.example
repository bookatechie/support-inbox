# Support Inbox Configuration

# Server
NODE_ENV=development
PORT=3001

# PostgreSQL Database
POSTGRES_USER=your-db-user
POSTGRES_PASSWORD=your-db-password
POSTGRES_HOST=your-db-host
POSTGRES_PORT=5432
POSTGRES_DB=your-db-name
POSTGRES_SSL=require

# File Storage
ATTACHMENTS_DIR=./data/attachments

# S3 Storage (optional - automatically enabled if credentials are provided)
# Leave blank to use local file storage
# If S3_BUCKET, S3_ACCESS_KEY_ID, and S3_SECRET_ACCESS_KEY are all set, S3 will be used automatically
S3_BUCKET=
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_PREFIX=support-inbox/attachments

# JWT Secret (change in production!)
JWT_SECRET=change-me-in-production-use-long-random-string

# Internal API Key (for automation scripts, webhooks, backend integrations)
# Generate with: openssl rand -base64 32
INTERNAL_API_KEY=sk_internal_change-me-in-production

# Default Admin User (created on first run if no users exist)
# IMPORTANT: Change these for production deployments!
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=admin123
DEFAULT_ADMIN_NAME=Admin User

# IMAP Configuration (Inbound Email)
IMAP_HOST=imap.dreamhost.com
IMAP_PORT=993
IMAP_TLS=true
IMAP_USER=support@yourdomain.com
IMAP_PASSWORD=your-email-password
IMAP_POLL_INTERVAL=1  # How often to check for new emails (in minutes)

# SMTP Configuration (Outbound Email)
SMTP_HOST=smtp.dreamhost.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=support@yourdomain.com
SMTP_PASSWORD=your-email-password
SMTP_FROM=support@yourdomain.com

# Customer Information API (optional)
# Webhook URL to fetch additional customer information
# Leave blank or comment out to disable this feature
CUSTOMER_INFO_API_URL=

# AI Response Generation API (optional)
# URL to generate AI-powered response suggestions
# Leave blank or comment out to disable AI response generation
AI_RESPONSE_API_URL=

# Webhook Notifications (optional)
# URL to send POST notifications for new tickets
# Leave blank or comment out to disable webhook notifications
WEBHOOK_URL=
